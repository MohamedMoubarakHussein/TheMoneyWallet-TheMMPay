export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
  wsUrl: 'ws://localhost:3000', // For WebSocket connections
  useMockData: true, // Set to true when backend is not available
  mockDelay: 500 // Simulate network delay for mock data
};